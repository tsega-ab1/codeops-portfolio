# Day 13 — CSS Foundations

## Files
- `exercise1-variables.html` — CSS custom properties via `var()`
- `exercise2-boxmodel.html` — `content-box` vs `border-box`
- `exercise3-specificity.html` — element/class/ID specificity comparison
- `exercise4-table.html` — zebra-striped table with `nth-child(even)`
- `exercise5-colors.html` — hex → rgb → hsl conversion, hover via lightness
- `business-profile-card/` — mini-project: a styled business card using
  CSS variables, `border-box`, Google Fonts, and a pseudo-element

## Concepts covered
- The cascade, inheritance, specificity
- The box model, `display`, margin/padding shorthand
- Typography, color systems, CSS variables, pseudo-classes/elements
