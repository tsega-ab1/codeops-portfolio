# Day 11 — How the Web Works & HTML Foundations

## Files
- `hello.html` — Exercises 1–4: full HTML5 boilerplate, a paragraph with
  `strong`/`em`, an image with meaningful `alt`, unordered/ordered lists,
  and a `nav` with a relative + absolute link.

## Concepts covered
- Client/server model, HTTP/HTTPS, DNS
- HTML document structure (`doctype`, `head`, `body`)
- Semantic elements, attributes, void elements
